<!DOCTYPE html>
<html lang="hi">

<head>
  <meta charset="UTF-8">
  <title>स्वश्रयी महिला सख सहकारी संस्था मर्यादित, इंदौर</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      margin: 30px;
      color: #000;
    }

    .receipt-container {
      width: 100%;
      max-width: 950px;
      margin: auto;
      border: 1px solid #000;
      padding: 20px;
    }

    .header {
      text-align: center;
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 20px;
    }

    .content-row {
      display: flex;
      justify-content: space-between;
    }

    .left-side {
      width: 45%;
      font-size: 16px;
    }

    .left-side .field {
      margin-bottom: 10px;
    }

    .left-side .field label {
      font-weight: bold;
      display: inline-block;
      width: 140px;
    }

    .right-side {
      width: 50%;
    }

    .note-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 16px;
    }

    .note-table th,
    .note-table td {
      border: 1px solid #000;
      padding: 6px;
      text-align: center;
    }

    .total-row td {
      font-weight: bold;
    }

    .amount-words {
      margin-top: 20px;
      font-size: 16px;
    }

    .footer {
      margin-top: 50px;
      display: flex;
      justify-content: space-between;
      font-size: 16px;
    }
  </style>
</head>

<body>

  <div class="receipt-container"  th:each="denom : ${denominationSummaryList}">
    <div class="header">स्वश्रयी महिला सख सहकारी संस्था मर्यादित, इंदौर</div>

    <div class="content-row">

      <!-- LEFT SIDE: DETAILS -->
      <div class="left-side">
        <div class="field">
          <label>सेवा मित्र का नाम:</label>
          <span th:text="${denom.agentName}">राधिका शर्मा</span>
        </div>
        <div class="field">
          <label>दिनांक:</label>
          <span th:text="${denom.denominationDate}">10/10/2025</span>
        </div>
        <div class="field">
          <label>टेक:</label>
          ____________________
        </div>
        <div class="field">
          <label>रसीद नम्बर:</label>
          <span th:text="${denom.receiptNo}">12345</span>
        </div>
        <div class="field">
          <label>ऑनलाइन:</label>
          ₹ <span th:text="${denom.onlineAmt}">500</span>
        </div>
        <div class="field">
          <label>बैंक में जमा:</label>
          ₹ <span th:text="${denom.offlineAmt + denom.onlineAmt}">4500</span>
        </div>
        <div class="field">
          <label>नगद:</label>
          ₹ <span th:text="${denom.offlineAmt}">4000</span>
        </div>
      </div>

      <!-- RIGHT SIDE: TABLE -->
      <div class="right-side">
        <table class="note-table">
          <thead>
            <tr>
              <th>नोट</th>
              <th>संख्या</th>
              <th>रकम</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>500x</td>
              <td th:text="${denom.r500}">2</td>
              <td th:text="${denom.r500 * 500}">1000</td>
            </tr>
            <tr>
              <td>200x</td>
              <td th:text="${denom.r200}">2</td>
              <td th:text="${denom.r200 * 200}">400</td>
            </tr>
            <tr>
              <td>100x</td>
              <td th:text="${denom.r100}">3</td>
              <td th:text="${denom.r100 * 100}">300</td>
            </tr>
            <tr>
              <td>50x</td>
              <td th:text="${denom.r50}">2</td>
              <td th:text="${denom.r50 * 50}">100</td>
            </tr>
            <tr>
              <td>10x</td>
              <td th:text="${denom.r10}">5</td>
              <td th:text="${denom.r10 * 10}">50</td>
            </tr>
            <tr>
              <td>5x</td>
              <td th:text="${denom.r5}">1</td>
              <td th:text="${denom.r5 * 5}">5</td>
            </tr>
            <tr>
              <td>2x</td>
              <td th:text="${denom.r2}">1</td>
              <td th:text="${denom.r2 * 2}">2</td>
            </tr>
            <tr>
              <td>1x</td>
              <td th:text="${denom.r1}">1</td>
              <td th:text="${denom.r1 * 1}">1</td>
            </tr>
            <tr class="total-row">
              <td colspan="2">₹ कुल राशि</td>
              <td th:text="${denom.totalAmount}">1858</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

    <!-- AMOUNT IN WORDS -->
    <!--<div class="amount-words">
      <strong>अक्षरी रुपये:</strong>
      <span th:text="${denom.amountInWords}">एक हजार आठ सौ अठावन रुपये</span>
    </div>-->

    <!-- FOOTER SIGNATURES -->
    <div class="footer">
      <div>जांचकर्ता के हस्ताक्षर</div>
      <div>कैश प्राप्तकर्ता के हस्ताक्षर</div>
      <div>कैश जमाकर्ता के हस्ताक्षर</div>
    </div>
  </div>

</body>

</html>
